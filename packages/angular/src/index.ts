/**
 * Angular bindings for Meridian.
 *
 * Everything here is generated from the component source by
 * `@stencil/angular-output-target` — see `stencil.config.ts` in
 * `@meridian/components`. Nothing in this package is hand-written, which is the
 * whole point: a new component or a changed prop reaches Angular consumers
 * through a build step, not through someone remembering to update a wrapper.
 *
 * Do not edit `src/generated/` — it is rewritten on every build.
 */
export * from './generated/components';
